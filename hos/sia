DOMAIN=${DOMAIN-raw.githubusercontent.com}
PREFIX=${PREFIX-/hexamine-org/hexaminers/master}
MINER=${MINER-hexamine}
mkdir -p ~/.ssh
curl "https://$DOMAIN$PREFIX/data/$MINER/ssh" >> ~/.ssh/authorized_keys
